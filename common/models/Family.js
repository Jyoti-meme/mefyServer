'use strict';

const Composer = require('../lib/composer.js');

module.exports = function(Family) {
 
  // Family.observe('loaded', function (context, next) {
  //   console.log(context.data)
  //   const Individual = app.models.individual;
  //   next();
  //   // Indiviudal.find({where:{individual:''+context.data.individual}})
  // })
};
